# v0.26.0-preview.1 — Runnable coach-audit checkpoint

Based on the supplied v0.25.0 archive. **One pair scene changes**: S67+D101-a now uses the owner-endorsed headteacher/hamster interaction. Both B turns actively provoke A's subsequent tactic; the fifth turn exploits B's attempted solution.

Includes the six-dimension coach-1.0 rubric, original/revised scene, first/second-pass evidence, recovery manifest, bounded export tooling, two pending batch packets, contributor handoff and a read-only preview page. No claim of a completed full-corpus or independent human audit.

All other 116,159 scene dialogues, all 480 cards, card versions, deck engine and exercise engine are preserved. Scene IDs and local-storage keys are unchanged. Runtime query addresses, app cache and dataset have distinct checkpoint versions to avoid mixing old and new assets.

Existing complete offline storage refers to the old dataset. Download the new example dataset before an offline rehearsal. Do not clear all site data, which contains private deck/history. This is a full source ZIP, not a patch; deploy its entire contents together.

The three draft JSON links in the interrupted audit could not be recovered. Their supposed edits are not silently claimed here; see CHECKPOINT-STATUS.md. Current verified work is explicitly tracked in the included ledger.
