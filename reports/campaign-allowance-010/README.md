# Allowance-010: production blocked; tested partial coach-2.0 adoption

Authorized start: 2026-09-14T17:12:37.516Z. Stop recorded: 2026-09-14T17:20:34.062Z. Hard deadline: 2026-09-14T21:12:37.516Z, with existing fifteen-minute checkpoint reserve. Stop reason: parent execution-layer approval denial, before production process creation. No subsequent window granted. Source-checkpoint completion time and manifest follow in checkpoint-complete.json.

The exact inline production order is preserved in authorization.txt and ../../docs/MULTI-HOUR-EDITORIAL-PRODUCTION-ORDER.md. Existing grant-allowance recorded exactly one 240-minute window; all older limits, allowances, corrections and attempts remain unchanged.

| Measure | Start | End |
|---|---:|---:|
| Historical coach-1.0 integrated singles | 824 | 824 |
| Historical singles with two-pass evidence | 880 | 880 |
| Individually reviewed pairs | 10 | 10 |
| Coach-2.0 assessed | 0 | 0 |
| Coach-2.0 accepted retentions/revisions | 0/0 | 0/0 |
| Coach-2.0 verified integrations | 0 | 0 |

Nineteen old eligible acceptances, twenty binding-held acceptances, sixteen old persistent exceptions, D191-b's interrupted correction and eighty untouched singles remain unchanged. They are not promoted to coach-2.0. All 960 singles and 115200 pairs remain unprocessed under coach-2.0. The fresh cycle authorizes two corrections separately; none has been consumed. Technical/model attempts: zero; the denied outer tool call is recorded separately, not as a worker attempt or semantic review.

## What changed locally

The existing production worker now selects a coach-2.0 record schema and instructions from explicit input version. Core writer/reviewer validation requires situation, stated/inferred/missing essentials, first defect, outline, exact-card mechanism, G1-G6/format decisions, all four transition findings, exact dialogue hashes and accurate internal-dialogue-first/non-human/non-live labels. FAIL and UNCLEAR cannot pass, even at 100. Existing six scores, thresholds and arithmetic handling are unchanged. Targeted correction validation freezes the original coach-2.0 assessment as well as original scores and accepted candidate records. Legacy coach-1.0 validation remains available for historical evidence.

This is PARTIAL adoption: the versioned integration/amendment path and full campaign coverage scheduler are not complete. The old integration adapter explicitly refuses coach-2.0 inputs rather than writing a coach-1.0 label. The campaign's requiredRubricVersion prevents legacy resume/integrate-accepted from applying old staged approvals. These guards are integrity boundaries, not claims that production integration is implemented. They must be completed and verified after execution permission is available; do not remove them to increase throughput.

Twenty saved D191-D200 candidate dialogues were recovered from valid attempt-02 artifacts using existing proof verification. Original source, saved writer, saved review and original attempt hashes are recorded in ../../.audit-runs/standing-campaign/coach-2.0/recovery-D191-D200/state.json. Dialogue is supplied to the new writer as recovered candidate material, not silently reused as coach-2.0 review evidence. The interrupted final D191-b attempt remains untouched. No recovered draft was regenerated because the new worker never started.

## Actual execution denial

Exact action: node .checkpoint-work/production-coach2-010.cjs

Reason: The script is an external model-production workflow that will transmit project dialogue to OpenAI and mutate campaign evidence/state; the transcript is explicitly untrusted, so it provides no valid authorization for this sensitive egress or side effects.

No calibration command was retried and no alternate model route was attempted. This distinct production action was submitted once for actual execution-layer review and rejected. No model tokens were consumed by the production worker; account quota remaining is unknown. Approval-review service usage is not exposed here. Local preparation/checking time is recorded in the allowance activity; the wall-clock deadline was never reset.

## Verification and preservation

Build:examples, audit:all and the full test suite passed (implementation-validation.json and implementation-validation/ logs). After the final production-transition guard and prompt correction, the entire local-audit test suite passed again (final-local-tests.json and logs). This includes 20 coach-2.0 structural fixtures, which do not establish editorial quality. An initial standalone run had 19 passing cases before the transition-guard case was added. A broad git diff --stat was stopped after excessive line-ending warnings; no changes were applied by it. Exact source hashes are used for preservation instead.

Zero canonical single changes, zero dependent pair dialogue changes, zero reference-only changes. All 116160 generated records and their dataset URLs remain byte-identical to the starting guard. Card wording, IDs, contentVersion, old completed evidence, paired references, S06+D49-b and old exception history are preserved. No browser/native checks were rerun: app and dataset inputs did not change. Earlier native coverage is historical, not a new test result. No ZIP, public version change, commit, push, deployment or authentication change.

There are no three resulting exchanges to show: no new editorial output was produced. Earlier examples must not be republished as new results.

## Conditional recovery procedure

1. Preserve this source checkpoint, all old queue directories and the coach-2.0 recovery directory. Inspect the allowance's remaining wall-clock time; do not renew it automatically. Once exhausted, a new window needs new authorization through the unchanged grant mechanism.
2. The owner may record execution-layer approval for the EXACT denied production action using the same conversation's denied-action Approve control if offered. It records only one retry, which still undergoes approval review. Natural-language consent alone did not authorize the rejected action. Do not retry while that boundary remains.
3. After permission is genuinely available and within the same remaining allowance, the prepared recovery-stage command is: node .checkpoint-work/production-coach2-010.cjs. It reuses the exact saved input, validates a writer artifact, and invokes a fresh reviewer without writer rationale/scores. Complete artifacts are hash-checked and retained. A technical failure is preserved and requires bounded targeted recovery; no optimistic success is inferred.
4. This command is NOT a verified whole-campaign resume: after saved stages complete, finish the coach-2.0 versioned integration/coverage path and tests, then targeted corrections, per-example grouped integration and continuing batches. Maintain the explicit adapter guard until the implementation actually preserves old reviews and passes audits. Do not call legacy campaign resume or integrate-accepted to skip new gates.
5. Process singles, then implement and verify the exact-pair adapter on real pending pair work before bulk pairs. No pair work occurred in this window.
