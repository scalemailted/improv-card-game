'use strict';
// Deliberately artificial evidence for orchestration tests, never a production review.
const {rubric,hash}=require('../../tools/coach-review-core.cjs');
const beats=['I brought our torn map to plan the walk.','The bridge is closed.','Then we can follow the river to the ferry.','The ferry takes bicycles too.','I will bring mine so we can ride home.'].map((text,i)=>({speaker:i%2?'B':'A',text}));
const pass=()=>({ratings:Object.fromEntries(rubric.criteria.map(c=>[c.id,{score:5,evidence:'FIXTURE ONLY: A1, B2, A3, B4 and A5 provide artificial test evidence.'}])),weightedTotal:100});
function input(){return {rubric,records:['a','b'].map(s=>({exampleId:'S01-single-'+s,key:'S01',reference:false,before:{id:'S01-single-'+s,format:'ABABA',beats:structuredClone(beats),exampleVersion:'0.25.0'},beforeHash:hash(beats),card:{id:'S01',type:'stance',title:'Fixture',instruction:'Fixture instruction only.',contentVersion:'1'}})),bindings:[{key:'S01',transfer:null,bespokePairs:[]}]};}
function writer(i=input()){const e='FIXTURE ONLY: A1 and A5 supply sufficient-length test evidence.';return {records:i.records.filter(r=>!r.reference).map(r=>({exampleId:r.exampleId,beforeHash:r.beforeHash,firstPass:pass(),beats:r.before.beats,decision:'retained',mechanism:e,confusableConcept:e,distinguishingEvidence:e,alternativeComparison:e,reason:e,reservation:e})),bindings:[{key:'S01',compatible:true,assessment:e}]};}
function review(i){const {gates}=require('../../tools/local-audit/core.cjs'),e='FIXTURE ONLY: B4 offers bicycles so A5 chooses to ride home.';return {records:i.records.filter(r=>!r.reference).map(r=>({exampleId:r.exampleId,candidateHash:hash(r.beats),secondPass:pass(),hardGates:Object.fromEntries(gates.map(g=>[g,true])),hardGateEvidence:Object.fromEntries(['b2CausesA3','b4CausesA5','cardContribution','sameGameHeightened'].map(k=>[k,e])),reservation:e,alternativeComparison:e,accept:true})),bindings:[{key:'S01',compatible:true,assessment:e}]};}
module.exports={beats,pass,input,writer,review};
