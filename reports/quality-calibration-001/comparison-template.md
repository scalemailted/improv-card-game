# Anonymous before/after comparison — not yet populated

No actual rejected example has been identified and no candidate rewrite has been produced. This is a blank collection template, not an executed randomized comparison. Do not reuse arbitrary sampled scenes as if they were the reported failures.

Once exact failures are provided, assign opaque labels to each original and candidate, randomize their display order, and store the mapping separately. Do not display “before,” “after,” source status, model rationale, scores, or human labels. Save the completed dialogue-only sheet before showing the exact card instruction sheet.

## Comparison slot 1

Anonymous displayed label:

Five spoken turns:

What are the speakers doing?

Relevant roles and wants/stakes:

First unclear line and why:

Which reply does not follow, and what is missing?

Essential facts the reader had to invent:

Card instruction(s), revealed only after the above is saved:

Distinctive card behavior / merged pair behavior visible in the lines:

Did revealing the cards repair missing basic context?

Remaining weakness:

## Comparison slot 2

Use the same fields for the independently labeled counterpart. Present it apart from its mate if immediate side-by-side comparison would reveal the intended rewrite. Preference, if requested, is collected only after both independent reads.

## Separate answer key — pending

Original scene ID/hash:

Original displayed label:

Candidate hash and displayed label:

Randomization record:

Actual human comment and first confusing line:

Candidate change and reviewer evidence references:

Development or held-out membership:

Do not put this key in the anonymous reviewer handout.
